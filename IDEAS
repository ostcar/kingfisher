* Add an attribute "log : Str" to the Response type, that gets printed by the platform
* Add some attribute the a write request, to forbit the host to save this request (for example, if it contains clear text passwords)
